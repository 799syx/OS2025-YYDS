# YYDS-OS 开发日志

**项目成员：** 徐悠然、吴雪琪、孙宇轩

**指导老师：** 田卫东、周红鹃

---

## 第四阶段（2025年12月上旬）- 高级调度器

### 🎉 项目重命名为 YYDS-OS

- 更新启动界面 ASCII 艺术字为 YYDS-OS 标志
- 采用青色 (Cyan) 配色方案，更加醒目美观
- 新增欢迎信息："Welcome to YYDS-OS v1.0 - Your Awesome OS!"

### 🚀 Embassy 异步调度器

实现了类似 Rust Embassy 框架的异步任务调度器，为内核提供协作式多任务支持：

**核心功能：**
- **4级优先级队列**：CRITICAL(0) > HIGH(1) > NORMAL(2) > LOW(3)
- **任务生命周期管理**：创建、销毁、状态追踪
- **协作式调度**：任务主动让出 CPU (yield)
- **延时等待**：embassy_delay_ms() 毫秒级延时
- **事件机制**：事件等待与触发
- **任务依赖**：支持任务间依赖关系
- **优先级提升**：防止低优先级任务饥饿
- **统计信息**：任务运行时间、上下文切换次数等

**新增系统调用 (14个)：**
| 系统调用 | 功能 |
|---------|------|
| `embassy_create_task` | 创建异步任务 |
| `embassy_create_task_named` | 创建命名任务 |
| `embassy_destroy_task` | 销毁任务 |
| `embassy_schedule` | 运行调度器 |
| `embassy_yield` | 让出 CPU |
| `embassy_delay_ms` | 延时等待 |
| `embassy_wait_event` | 等待事件 |
| `embassy_trigger_event` | 触发事件 |
| `embassy_add_dependency` | 添加任务依赖 |
| `embassy_set_task_group` | 设置任务组 |
| `embassy_boost_priority` | 提升优先级 |
| `embassy_get_task_stats` | 获取任务统计 |
| `embassy_get_global_stats` | 获取全局统计 |
| `embassy_print_stats` | 打印统计信息 |

**新增文件：**
- `kernel/proc/embassy.c` - Embassy 调度器实现
- `kernel/proc/embassy.h` - Embassy 头文件定义
- `kernel/sys_embassy.c` - Embassy 系统调用接口
- `user/test/embassy_test.c` - Embassy 测试程序

### 🔧 Bug 修复

- 修复 `sysmon.c` 中未使用变量 `prev_idle_ticks` 导致的编译错误
- 修复头文件包含路径问题
- 完善系统调用注册流程

---

## 第五阶段（2025年12月中旬）- 内存管理增强

### 🧱 Slab 分配器

高效管理小对象内存分配：

- **5种大小类别**：32, 64, 128, 256, 512 字节
- **缓存机制**：减少频繁的页面分配
- **统计信息**：分配次数、缓存命中率等
- **内存收缩**：支持释放空闲页面

### 🗺️ VMA (Virtual Memory Area) 增强

增强的虚拟内存区域管理：

- **统一管理**：代码、数据、堆、栈、mmap 区域
- **缺页处理**：懒加载和 COW 支持
- **统计信息**：页面错误、COW 错误计数

---

## 第六阶段（2025年12月下旬）- 多系统特性融合

### 🎯 QoS (Quality of Service) 调度器

参考鸿蒙 QoS 设计，实现基于服务质量的任务调度：

**6级 QoS 优先级：**
| 级别 | 名称 | 说明 |
|------|------|------|
| 0 | USER_INTERACTIVE | 用户交互（最高优先级）|
| 1 | DEADLINE_REQUEST | 截止时间请求 |
| 2 | USER_INITIATED | 用户发起 |
| 3 | DEFAULT | 默认级别 |
| 4 | UTILITY | 实用工具 |
| 5 | BACKGROUND | 后台任务（最低优先级）|

**核心特性：**
- **自动优先级提升**：防止低优先级任务饥饿
- **截止时间感知**：支持任务截止时间设置
- **I/O 密集型提升**：I/O 完成后临时提升优先级
- **统计信息**：调度次数、优先级提升、截止时间错过等

### 📁 sysfs 虚拟文件系统

以文件形式暴露内核信息，类似 Linux 的 /sys：

```
/sys/
├── kernel/
│   ├── version    - 内核版本
│   ├── hostname   - 主机名
│   └── uptime     - 运行时间
├── mem/
│   ├── total      - 总内存
│   ├── free       - 空闲内存
│   └── used       - 已用内存
├── cpu/
│   ├── count      - CPU 数量
│   └── info       - CPU 信息
└── proc           - 进程列表
```

### 📊 新增系统调用 (8个)

| 系统调用 | 功能 |
|---------|------|
| `qos_set` | 设置进程 QoS 级别 |
| `qos_get` | 获取进程 QoS 级别 |
| `qos_set_deadline` | 设置截止时间 |
| `qos_stats` | 获取 QoS 统计 |
| `qos_print` | 打印 QoS 统计 |
| `sysfs_read` | 读取 sysfs 文件 |
| `sysfs_list` | 列出 sysfs 目录 |
| `slab_stats` | 打印 Slab 统计 |

### 📁 新增文件

- `kernel/proc/qos.c` / `qos.h` - QoS 调度器
- `kernel/sys_qos.c` - QoS 系统调用
- `kernel/fs/sysfs.c` / `sysfs.h` - sysfs 实现
- `kernel/sys_sysfs.c` - sysfs 系统调用
- `kernel/mm/slab.c` / `slab.h` - Slab 分配器
- `kernel/mm/vma.c` / `vma.h` - VMA 管理
- `kernel/lib/printf.c` - 添加 snprintf 函数
- `user/test/qostest.c` - 新功能测试程序

### 🌐 HMDFS 分布式文件系统

参考鸿蒙 HMDFS 设计，实现跨设备文件共享：

**核心模块：**

1. **设备管理** (~500行)
   - 设备注册与发现
   - 设备状态监控（在线/离线/同步中）
   - 支持6种设备类型：本地、手机、平板、电脑、手表、电视
   - 设备信任级别管理

2. **文件共享** (~400行)
   - 文件共享与取消共享
   - 版本控制与追踪
   - 副本管理

3. **冲突处理** (~300行)
   - 冲突检测（同时修改、删除、重命名）
   - 4种解决策略：最新优先、最大优先、手动、合并
   - 冲突记录与解决

4. **缓存优化** (~200行)
   - 本地缓存管理
   - 缓存失效与刷新
   - 脏数据回写
   - LRU 淘汰策略

5. **同步管理**
   - 自动/手动同步
   - 同步状态追踪
   - 统计信息

**新增系统调用 (8个)：**

| 系统调用 | 功能 |
|---------|------|
| `hmdfs_register_device` | 注册设备 |
| `hmdfs_device_offline` | 设备下线 |
| `hmdfs_list_devices` | 列出设备 |
| `hmdfs_share` | 共享文件 |
| `hmdfs_unshare` | 取消共享 |
| `hmdfs_list_shared` | 列出共享文件 |
| `hmdfs_sync` | 触发同步 |
| `hmdfs_stats` | 打印统计 |

**新增文件：**
- `kernel/fs/hmdfs.c` / `hmdfs.h` - HMDFS 核心实现
- `kernel/sys_hmdfs.c` - HMDFS 系统调用
- `user/test/hmdfstest.c` - HMDFS 测试程序

### 📱 Binder IPC (Android 风格)

实现 Android 风格的高效进程间通信机制：

**核心功能：**
- **服务注册与发现**：服务名到句柄的映射
- **引用计数**：自动管理服务生命周期
- **事务管理**：同步/异步调用支持
- **统计信息**：事务计数、调用统计

**新增系统调用 (5个)：**
| 系统调用 | 功能 |
|---------|------|
| `binder_register` | 注册服务 |
| `binder_lookup` | 查找服务 |
| `binder_release` | 释放服务引用 |
| `binder_list` | 列出所有服务 |
| `binder_stats` | 打印统计信息 |

### 🐧 cgroups 资源控制 (Linux 风格)

实现 Linux 风格的资源控制组：

**控制器类型：**
- **CPU 控制器**：CPU 份额、配额、周期
- **内存控制器**：内存限制、使用统计、OOM 计数
- **I/O 控制器**：读写速率限制、IOPS 限制
- **进程数控制器**：最大进程数限制

**核心功能：**
- 层级化 cgroup 结构
- 资源限制继承
- 进程加入/移除
- 资源使用监控

**新增系统调用 (7个)：**
| 系统调用 | 功能 |
|---------|------|
| `cgroup_create` | 创建 cgroup |
| `cgroup_delete` | 删除 cgroup |
| `cgroup_attach` | 将进程加入 cgroup |
| `cgroup_set_memory` | 设置内存限制 |
| `cgroup_set_cpu` | 设置 CPU 份额 |
| `cgroup_list` | 列出 cgroups |
| `cgroups_stats` | 打印统计信息 |

### 🔷 Ability 框架 (鸿蒙风格)

实现鸿蒙风格的应用能力框架：

**Ability 类型：**
- **PAGE (FA)**：页面能力，有 UI 界面
- **SERVICE (PA)**：服务能力，后台运行
- **DATA (PA)**：数据能力，数据访问

**生命周期管理：**
- INITIAL → INACTIVE → ACTIVE → BACKGROUND
- 页面栈管理（前进/后退导航）
- 生命周期事件追踪

**新增系统调用 (7个)：**
| 系统调用 | 功能 |
|---------|------|
| `ability_register` | 注册 Ability |
| `ability_start` | 启动 Ability |
| `ability_stop` | 停止 Ability |
| `ability_destroy` | 销毁 Ability |
| `ability_back` | 返回上一页 |
| `ability_list` | 列出 Abilities |
| `ability_stats` | 打印统计信息 |

### 📁 新增文件

- `kernel/proc/binder.c` / `binder.h` - Binder IPC 实现
- `kernel/sys_binder.c` - Binder 系统调用
- `kernel/proc/cgroups.c` / `cgroups.h` - cgroups 实现
- `kernel/sys_cgroups.c` - cgroups 系统调用
- `kernel/proc/ability.c` / `ability.h` - Ability 框架实现
- `kernel/sys_ability.c` - Ability 系统调用
- `user/test/osfeatures_test.c` - 多系统特性测试程序

---

## 第七阶段（2026年1月）- 高级内核功能

### 🔐 权能系统 (Capability)

融合 Linux CAP_* 和鸿蒙扩展权能，实现细粒度权限控制：
- 30种权能位定义
- 权能继承与传递
- 进程权能检查

### ⏱️ 实时调度

实现多种实时调度策略：
- SCHED_FIFO：先进先出实时调度
- SCHED_RR：时间片轮转实时调度
- SCHED_DEADLINE：EDF 最早截止时间优先

### 🧊 进程冻结 (Freezer)

Android 风格的进程冻结机制：
- 后台进程自动冻结
- 低内存时优先冻结
- 支持手动冻结/解冻

### 🔒 Futex 快速互斥锁

Linux 风格的用户态快速互斥锁：
- 减少系统调用开销
- 支持等待和唤醒操作

### 📊 内核性能分析器

内核级性能分析工具：
- 上下文切换统计
- 系统调用计数
- CPU 使用率监控

---

## 第三阶段（2025年11月下旬）- Shell增强与工具

  - **信号系统：** 实现完整的信号处理机制
    - `SYS_signal`: 注册信号处理函数
    - `SYS_sigkill`: 向进程发送信号
    - `SYS_pause`: 暂停进程等待信号
    - 支持 Ctrl+C 发送 SIGINT 信号中断前台进程
  
  - **网络系统调用扩展：** 完善 Socket API
    - `SYS_socket`: 创建套接字
    - `SYS_bind`: 绑定端口
    - `SYS_listen`: 监听连接
    - `SYS_accept`: 接受连接
    - `SYS_send`: 发送数据
    - `SYS_recv`: 接收数据
  
  - **系统工具：** 添加系统信息查询功能
    - `SYS_gettime`: 获取系统时间
    - `SYS_hostname`: 获取/设置主机名
    - `SYS_getenv`/`SYS_setenv`: 环境变量管理
    - `SYS_procinfo`: 获取进程详细信息
  
  - **文件系统增强：**
    - `SYS_kstat`: 获取文件状态信息
    - `SYS_truncate`: 截断文件
    - `SYS_dup2`: 复制文件描述符到指定位置
    - `SYS_getdents`: 读取目录项
  
  - **进程管理增强：**
    - `SYS_nice`: 调整进程优先级
    - `SYS_times`: 获取进程CPU时间统计
    - `SYS_getppid`: 获取父进程ID
    - `SYS_setpgid`/`SYS_getpgid`: 进程组ID管理
    - `SYS_setsid`/`SYS_getsid`: 会话ID管理
  
  - **用户/组管理：** 实现简单的用户认证系统
    - `SYS_getgid`/`SYS_setgid`: 组ID管理
    - `SYS_login`: 用户登录认证
    - `SYS_sudo`: 权限提升（需root密码）
    - 内置用户数据库：root、user、guest
  
  - **控制台增强：**
    - `SYS_consctl`: 控制台模式切换（行缓冲/原始模式）
    - 支持原始模式下捕获特殊按键
  
  - **Shell增强：** 大幅改进交互体验
    - 命令历史：使用↑↓键浏览历史命令
    - Tab自动补全：文件名自动补全
    - 后台任务：支持 `&` 后台运行，`jobs` 查看任务
    - 内置命令：`history`、`jobs`、`exit`
  
  - **Unix常用命令：** 新增多个用户程序
    - `cp`: 复制文件
    - `mv`: 移动/重命名文件
    - `touch`: 创建空文件或更新时间戳
    - `head`/`tail`: 查看文件头部/尾部
    - `clear`: 清屏
    - `date`: 显示日期时间
    - `ps`: 显示进程状态
    - `env`/`export`: 环境变量管理
    - `hostname`: 显示主机名
  
  - **实用小工具：** 新增 utiltest 测试程序
    - `factor`: 分解质因数
    - `seq`: 打印数字序列
    - `yes`: 重复输出字符串
    - `cal`: 显示日历
    - `banner`: ASCII艺术字

---

## **项目总结**

截至目前，YYDS-OS 已实现 **144个系统调用**（xv6原有21个），涵盖：
- 进程管理与调度（MLFQ多级反馈队列调度器、Embassy异步调度器、QoS调度器）
- 内存管理（COW、懒分配、MMAP、Slab分配器、VMA管理、cgroups资源控制）
- 文件系统（大文件支持、符号链接、权限控制、sysfs虚拟文件系统、HMDFS分布式文件系统）
- 网络设备（e1000驱动、UDP/IP、Socket API）
- 进程间通信（共享内存、消息队列、信号量、Binder IPC）
- 用户管理（登录认证、权限提升）
- Shell增强（历史、补全、后台任务）
- 异步任务调度（Embassy框架）
- 服务质量调度（QoS框架）
- 分布式文件系统（HMDFS框架）
- 应用能力框架（Ability框架）

**多系统特性融合：**
- 🤖 **Android**: Binder IPC 进程间通信
- 🐧 **Linux**: cgroups 资源控制组
- 🔷 **HarmonyOS**: Ability 应用能力框架、HMDFS 分布式文件系统、QoS 调度

系统测试程序位于 `/user/test` 目录下，用户程序位于 `/user/program` 目录下。
