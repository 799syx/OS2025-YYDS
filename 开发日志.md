# 项目开发进展 -OS

**项目成员：** 徐悠然、吴雪琪、孙宇轩

**指导老师：** 田卫东、周红鹃


##  2026年1月

  - **信号系统：** 实现完整的信号处理机制
    - `SYS_signal`: 注册信号处理函数
    - `SYS_sigkill`: 向进程发送信号
    - `SYS_pause`: 暂停进程等待信号
    - 支持 Ctrl+C 发送 SIGINT 信号中断前台进程
  
  - **网络系统调用扩展：** 完善 Socket API
    - `SYS_socket`: 创建套接字
    - `SYS_bind`: 绑定端口
    - `SYS_listen`: 监听连接
    - `SYS_accept`: 接受连接
    - `SYS_send`: 发送数据
    - `SYS_recv`: 接收数据
  
  - **系统工具：** 添加系统信息查询功能
    - `SYS_gettime`: 获取系统时间
    - `SYS_hostname`: 获取/设置主机名
    - `SYS_getenv`/`SYS_setenv`: 环境变量管理
    - `SYS_procinfo`: 获取进程详细信息
  
  - **文件系统增强：**
    - `SYS_kstat`: 获取文件状态信息
    - `SYS_truncate`: 截断文件
    - `SYS_dup2`: 复制文件描述符到指定位置
    - `SYS_getdents`: 读取目录项
  
  - **进程管理增强：**
    - `SYS_nice`: 调整进程优先级
    - `SYS_times`: 获取进程CPU时间统计
    - `SYS_getppid`: 获取父进程ID
    - `SYS_setpgid`/`SYS_getpgid`: 进程组ID管理
    - `SYS_setsid`/`SYS_getsid`: 会话ID管理
  
  - **用户/组管理：** 实现简单的用户认证系统
    - `SYS_getgid`/`SYS_setgid`: 组ID管理
    - `SYS_login`: 用户登录认证
    - `SYS_sudo`: 权限提升（需root密码）
    - 内置用户数据库：root、user、guest
  
  - **控制台增强：**
    - `SYS_consctl`: 控制台模式切换（行缓冲/原始模式）
    - 支持原始模式下捕获特殊按键
  
  - **Shell增强：** 大幅改进交互体验
    - 命令历史：使用↑↓键浏览历史命令
    - Tab自动补全：文件名自动补全
    - 后台任务：支持 `&` 后台运行，`jobs` 查看任务
    - 内置命令：`history`、`jobs`、`exit`
  
  - **Unix常用命令：** 新增多个用户程序
    - `cp`: 复制文件
    - `mv`: 移动/重命名文件
    - `touch`: 创建空文件或更新时间戳
    - `head`/`tail`: 查看文件头部/尾部
    - `clear`: 清屏
    - `date`: 显示日期时间
    - `ps`: 显示进程状态
    - `env`/`export`: 环境变量管理
    - `hostname`: 显示主机名
  
  - **实用小工具：** 新增 utiltest 测试程序
    - `factor`: 分解质因数
    - `seq`: 打印数字序列
    - `yes`: 重复输出字符串
    - `cal`: 显示日历
    - `banner`: ASCII艺术字

---

## **项目总结**

截至目前，Bird-OS 已实现 **95个系统调用**（xv6原有21个），涵盖：
- 进程管理与调度（MLFQ多级反馈队列调度器）
- 内存管理（COW、懒分配、MMAP）
- 文件系统（大文件支持、符号链接、权限控制）
- 网络设备（e1000驱动、UDP/IP、Socket API）
- 进程间通信（共享内存、消息队列、信号量）
- 用户管理（登录认证、权限提升）
- Shell增强（历史、补全、后台任务）

系统测试程序位于 `/user/test` 目录下，用户程序位于 `/user/program` 目录下。
