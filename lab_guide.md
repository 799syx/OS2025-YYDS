# 页面置换算法实验指导书

## 实验一：基础页面置换算法实现

### 1.1 实验目的

1. 理解页面置换的基本原理
2. 掌握FIFO、LRU、OPT三种经典算法的实现
3. 学会分析算法的性能指标

### 1.2 实验原理

#### 页面置换的背景
当物理内存不足时，操作系统需要将某些页面从内存中移除（置换），为新页面腾出空间。选择合适的置换策略对系统性能至关重要。

#### FIFO算法
- **思想**：选择最早进入内存的页面进行置换
- **实现要点**：
  - 使用队列记录页面进入顺序
  - 替换时选择队列头部的页面

#### LRU算法
- **思想**：选择最久未使用的页面进行置换
- **实现要点**：
  - 每次访问页面时更新其访问时间
  - 替换时选择访问时间最早的页面

#### OPT算法
- **思想**：选择未来最久不会被访问的页面（理论最优）
- **实现要点**：
  - 需要预知完整的访问序列
  - 对每个页面，查找其下一次访问的时间
  - 选择下一次访问时间最远的页面

### 1.3 实验步骤

#### 步骤1：理解代码结构
1. 阅读 `page_replacement.h`，理解数据结构和函数接口
2. 查看 `page_replacement.c` 中的算法实现
3. 理解 `PageFrame` 结构体的各个字段含义

#### 步骤2：实现FIFO算法
参考代码中的 `fifo_algorithm` 函数：
```c
// 关键点：
// 1. 使用 next_replace 指针记录下一个要替换的帧
// 2. 替换后更新指针：next_replace = (next_replace + 1) % frame_count
```

#### 步骤3：实现LRU算法
参考代码中的 `lru_algorithm` 函数：
```c
// 关键点：
// 1. 每次访问页面时更新 access_time
// 2. 替换时查找 access_time 最小的页面
```

#### 步骤4：实现OPT算法
参考代码中的 `opt_algorithm` 函数：
```c
// 关键点：
// 1. 对每个页面，遍历后续序列查找下一次访问位置
// 2. 选择下一次访问最远的页面
```

#### 步骤5：测试验证
运行测试程序：
```bash
make
./test
```

观察输出结果，验证算法正确性。

### 1.4 思考题

1. FIFO算法为什么会出现Belady异常？如何验证？
2. LRU算法的时间复杂度是多少？如何优化？
3. OPT算法为什么在实际系统中无法使用？

## 实验二：改进算法实现

### 2.1 实验目的

1. 掌握Clock算法和LFU算法的实现
2. 理解改进算法的设计思路
3. 学会分析算法在不同场景下的性能

### 2.2 实验原理

#### Clock算法
- **思想**：使用循环队列和引用位，类似时钟指针扫描
- **实现要点**：
  - 维护一个时钟指针（clock_hand）
  - 页面被访问时设置引用位为1
  - 替换时扫描，清除引用位，选择第一个引用位为0的页面

#### LFU算法
- **思想**：选择访问频率最低的页面
- **实现要点**：
  - 维护每个页面的访问计数（frequency）
  - 每次访问时增加计数
  - 替换时选择计数最小的页面

#### 改进的Clock算法
- **思想**：在Clock基础上考虑修改位，减少写回操作
- **实现要点**：
  - 按 (0,0) -> (0,1) -> (1,0) -> (1,1) 的顺序查找
  - 优先替换未修改的页面

### 2.3 实验步骤

#### 步骤1：实现Clock算法
1. 理解引用位的作用
2. 实现时钟指针的循环扫描
3. 处理引用位的设置和清除

#### 步骤2：实现LFU算法
1. 理解频率计数的维护
2. 实现最小频率页面的查找

#### 步骤3：实现改进的Clock算法
1. 理解修改位的意义
2. 实现多轮扫描策略

#### 步骤4：性能对比
运行基准测试：
```bash
./benchmark
```

对比不同算法在不同场景下的性能。

### 2.4 思考题

1. Clock算法相比LRU有什么优势？
2. LFU算法在什么场景下性能较好？
3. 改进的Clock算法如何减少I/O操作？

## 实验三：自适应算法设计（创新实验）

### 3.1 实验目的

1. 理解自适应算法的设计思想
2. 学会分析页面访问模式
3. 掌握算法选择的策略

### 3.2 实验原理

#### 局部性原理
程序执行具有时间局部性和空间局部性：
- **时间局部性**：最近访问的页面很可能再次被访问
- **空间局部性**：访问的页面附近的页面也可能被访问

#### 局部性量化
使用唯一页面数与总访问数的比值作为局部性指标：
- 比值小（<0.3）：局部性好，适合LRU
- 比值中等（0.3-0.6）：适合Clock
- 比值大（>0.6）：局部性差，适合LFU

#### 自适应策略
根据计算的局部性指标，动态选择最适合的算法。

### 3.3 实验步骤

#### 步骤1：实现局部性计算
```c
// 计算唯一页面数
// 计算局部性指标：locality = unique_pages / total_accesses
```

#### 步骤2：实现算法选择逻辑
```c
if (locality < 0.3) {
    // 使用LRU
} else if (locality < 0.6) {
    // 使用Clock
} else {
    // 使用LFU
}
```

#### 步骤3：测试验证
使用不同局部性的测试序列，验证自适应算法的选择是否正确。

### 3.4 扩展实验

1. **改进局部性计算**：考虑时间窗口，只分析最近N次访问
2. **多算法融合**：不是选择单一算法，而是结合多种算法的优点
3. **性能监控**：实时监控算法性能，动态调整策略

### 3.5 思考题

1. 如何更准确地量化页面访问的局部性？
2. 除了局部性，还有哪些因素可以用于算法选择？
3. 如何设计一个通用的自适应框架？

## 实验四：性能测试与分析

### 4.1 实验目的

1. 学会设计测试用例
2. 掌握性能分析方法
3. 学会撰写测试报告

### 4.2 测试用例设计

#### 测试用例1：标准序列
- 目的：验证算法基本正确性
- 设计：使用经典的测试序列

#### 测试用例2：Belady异常
- 目的：验证FIFO的异常现象
- 设计：使用特定序列，对比不同帧数下的缺页率

#### 测试用例3：局部性测试
- 目的：测试自适应算法
- 设计：生成不同局部性的访问序列

#### 测试用例4：大规模测试
- 目的：测试算法在大规模数据下的性能
- 设计：生成长序列，测试执行时间

### 4.3 性能指标

- **缺页次数**：越少越好
- **命中率**：越高越好
- **执行时间**：用于算法效率对比

### 4.4 测试方法

1. 运行测试程序，记录结果
2. 运行基准测试，获取性能数据
3. 对比分析不同算法的表现
4. 绘制性能对比图表

### 4.5 实验报告要求

1. **测试环境**：说明测试的硬件和软件环境
2. **测试用例**：列出所有测试用例和预期结果
3. **测试结果**：展示实际的测试数据
4. **结果分析**：分析各算法的优缺点
5. **结论**：总结测试发现

## 调试技巧

### 常见问题

1. **内存泄漏**：确保释放所有分配的内存
2. **数组越界**：检查数组索引是否合法
3. **逻辑错误**：使用调试器逐步跟踪执行

### 调试方法

1. **打印调试**：在关键位置打印变量值
2. **断点调试**：使用GDB设置断点
3. **单元测试**：为每个函数编写测试用例

## 实验总结

通过本实验，你应该：
1. ✅ 理解页面置换的基本原理
2. ✅ 掌握多种置换算法的实现
3. ✅ 学会分析和优化算法性能
4. ✅ 具备设计和实现新算法的能力

## 与实验九的对比

### 本实验的完善性

本优化项目基于**实验九：页面置换算法与动态内存分配**，在以下方面有显著完善：

1. **算法实现更全面**：
   - 实验九要求：OPT、FIFO、LRU（必须）+ LFU、Clock、PBA（可选，无指导）
   - 本项目实现：7种算法（全部实现+创新算法）
2. **测试框架更完善**：包含多种测试用例和性能基准测试（实验九只有固定序列）
3. **代码质量更高**：详细的Doxygen风格注释和模块化设计
4. **文档更完整**：设计文档、实验指导、对比分析文档（实验九文档较简单）

### 本实验的创新点

1. **改进的Clock算法**：考虑修改位，减少I/O操作（实验九未提及）
2. **自适应算法**：根据访问模式动态选择最优算法（核心创新，实验九未提及）
3. **完整的性能测试框架**：自动化的性能评估和对比（实验九无要求）
4. **递进式实验设计**：从基础到创新，循序渐进（实验九为单一任务）

详细的对比如创新点分析请参考：
- `comparison.md` - 与现有实验的对比分析
- `experiment_nine_comparison.md` - 与实验九的详细对比

## 参考资料

1. 项目源代码和注释
2. 设计文档（design_doc.md）
3. 对比分析文档（comparison.md）
4. 《操作系统概念》相关章节
5. 在线算法可视化工具

